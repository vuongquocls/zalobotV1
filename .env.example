# === Tên bot (chỉ trả lời khi gọi đích danh, nhiều tên cách bằng dấu phẩy) ===
BOT_NAME=Nhân Viên Mới Yok Đôn

# === LLM Providers (ưu tiên từ trên xuống) ===

# OpenRouter (miễn phí với free models)
OPENROUTER_API_KEY=your_openrouter_api_key
OPENROUTER_MODEL=meta-llama/llama-4-maverick:free

# Groq (miễn phí, 14400 req/ngày)
GROQ_API_KEY=your_groq_api_key
GROQ_MODEL=llama-3.3-70b-versatile

# Gemini (fallback cuối)
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.0-flash

# === Google Sheet (phải share "Anyone with the link") ===
GOOGLE_SHEET_ID=1tdgynCsD8b3JjptyAvXNbZtnF5Ng6ChaFxQO4uHDYK8
GOOGLE_SHEET_GID=1629674940

# === Zalo Config ===
# Tên chính xác của nhóm Zalo (bot sẽ tìm nhóm này để gửi nhắc)
ZALO_GROUP_NAME=Nhom Truyen Thong

# === Lịch nhắc tự động (24h format) ===
REMINDER_HOUR=8
REMINDER_MINUTE=0

# === Server Config ===
# Chế độ không cửa sổ (ưu tiên cho VPS)
HEADLESS=false
